{
    "name": "weinre-heroku",
    "version": "1.0.0",
    "scripts": {
      "start": "node node_modules/.bin/weinre --httpPort $PORT --boundHost -all- --verbose"
    },
    "dependencies": {
        "weinre": "3.x"
    }
}